# Claude project context

This repo is a **Neovim configuration** (LazyVim + lazy.nvim), not an application. There is no top-level build or test.

**Primary context document for agents:** [../agent-context.md](../agent-context.md)

Use `agent-context.md` for:
- One-sentence repo summary
- Entry point and layout (init.lua, lua/config, lua/plugins, lua/util, ftplugin)
- Plugin index and conventions
- Conventions for agents (no hand-edit of lazy-lock.json, where to add behavior)
- Quick file index

For detailed workflows and structure: [../WARP.md](../WARP.md).  
For AI assistant style (edits, tests, Neovim): [../avante.md](../avante.md).
